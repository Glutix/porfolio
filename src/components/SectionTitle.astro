---
interface Props {
  id: string;
  title: string;
  icon: any;
}

const { id, title, icon: Icon }: Props = Astro.props;
---

<section>
  <span><Icon class="icon" /></span>
  <h2 id={id}>{title}</h2>
</section>

<style>
  section {
    display: flex;
    align-items: center;
    gap: 1rem;
    height: fit-content;
  }

  .icon {
    width: 30px;
    height: 30px;
    color: var(--text-title);
  }

  span {
    display: flex;
    align-items: center;
  }
</style>
